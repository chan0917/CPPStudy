#include <iostream>
#include "Chapter3.h"
using namespace std;

void C3::A()
{
	/*C3::Car A = { "A", 100, 0 };
	A.Accel();
	A.Accel();
	A.ShowCarState();*/
	/*void C3::Car()
	{
		if (fuelGauge <= 0)
			return;
		else
			fuelGauge -= FUEL_STEP;

		if (curSpeed + ACC_STEP >= MAX_SPD)
		{
			curSpeed = MAX_SPD;
			return;
		}

		curSpeed += ACC_STEP;

	}*/
	
}